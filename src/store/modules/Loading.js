const state = {
  loading: false
};

const mutations = {
  LOADING(state, bool) {
    state.loading = bool;
  }
};

const actions = {};

const getters = {};

export default {
  state,
  getters,
  actions,
  mutations
};
